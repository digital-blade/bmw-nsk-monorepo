{
  "name": "database",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "ts-node prisma/index.ts && prisma generate",
    "build": "tsup src/index.ts --format cjs --dts",
    "start": "pnpm run build --watch",
    "test:lint": "eslint \"prisma/**/*.ts\"",
    "test:lint-report": "pnpm test:lint -f json -o eslint-report.json"
  },
  "dependencies": {
    "@prisma/client": "^4.9.0",
    "prisma": "^4.9.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "prisma-zod-generator": "^0.8.11",
    "schemix": "^1.8.5",
    "ts-node": "^10.0.0",
    "tsconfig": "workspace:*",
    "tsup": "^6.5.0",
    "typescript": "^4.9.4",
    "zod": "^3.20.2"
  },
  "peerDependencies": {
    "zod": "^3.20.2"
  }
}
